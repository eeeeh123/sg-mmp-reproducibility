# 正式实验前审计（revision-full-v4）

审计结论：内部实验代码已具备上机条件，但只有在服务器完成模型/数据预置、四模型 smoke test、双卡/双进程预检并得到 `"ready": true` 后才是 **GO**。内部实验跑完不等于可投稿；外部 TaCQ/HAWQ-V2 与人工双人标注通过 `resubmission` 门后才是投稿级 **GO**。

## 审稿问题到证据的闭环

| 审稿问题 | v4 处理 | 失败即停止的检查 | 当前状态 |
|---|---|---|---|
| 用测试子集选层且与评估重叠 | 选层只用 3×256 个互斥 GSM8K train 样本；最终统一用完整 1,319 test | 协议锁检查 train/test、互斥索引与完整 ID | 代码已解决，数字待跑 |
| 300/500 子集不能支撑结论 | 所有 headline、W4/W5/W6、SG 和控制实验改用全集 | 每个样本文件必须恰有 0–1318，重复/缺失均失败 | 代码已解决，旧数字禁用 |
| 不同任务格式造成混杂 | 同一 GSM8K 项目同时做生成和四选一；另加数学/非数学、生成/判别面板 | 固定 MCQ 清单、完整面板、无第二套 GSM8K headline | 代码已解决，数字待跑 |
| 只报绝对准确率不公平 | 同时报绝对下降、相对错误率增幅、相对 FP16 的归一化恢复 | 分析必须读到 FP16/W4/SG 三点 | 代码已解决 |
| 缺少直接混合精度替代方法 | 内部加入均匀 W5/W6、角色匹配、Hessian 匹配；外部要求官方 TaCQ/HAWQ-V2 | `resubmission` 缺任一 Qwen 模型×方法即失败 | 接口已解决，外部数字/适配待做 |
| 敏感层只在少量样本/少量随机集上成立 | 三个独立 train screen 与校准种子；2,000 次选层 bootstrap；每个主模型 30+30 唯一随机集 | 层/模块集合必须唯一、预登记并哈希 | 代码已解决，数字待跑 |
| 量化与校准随机性不足 | WikiText 种子 41/97/193；每模型/种子 W4/W5/W6/SG；种子×样本两级 bootstrap | 少任一 run 或改变校准配置即失败 | 代码已解决，数字待跑 |
| q/k/v 的收益可能只是预算或模块位置 | 纯 qkv/o/FFN 曲线，加同预算 qkv/o/FFN 优先与 Hessian 控制 | 每模型七项控制及真实平均 bit 元数据齐全 | 代码已解决，数字待跑 |
| 跨模型直接转移层无效 | 四个模型分别原生 screen，禁止沿用 Qwen 层 | 每模型必须有自己的 screen/selection 与模型哈希 | 代码已解决 |
| “误差传播”只是相关性 | 固定 200 项，逐层替换 block、attention、MLP；主指标为 final-answer token NLL | 每项必须覆盖全部三类×全部层并做 Holm 校正 | 代码已解决，数字待跑；不显著则删因果表述 |
| 错误类型分析不充分 | 全集自动审计；每主模型 200 例盲法；A/B 输出分别标注；固定预登记 40 例双编码 | 两输出共用一个标签的旧表被拒绝；方法解盲计数与 kappa 必须存在 | 代码已解决，人工工作待做 |
| 数值/命名/协议不一致 | 冻结模型 commit 和权重哈希、数据 cache 哈希、batch、生成长度、任务列表及协议版本 | 旧版本、重复 ID、断点混 batch、缓存变化全部失败 | 代码已解决 |

## 旧实验如何处理

| 旧材料 | 能否支持修改稿结论 | 处置 |
|---|---|---|
| GSM8K-300 敏感层结果 | 否；使用了测试数据且样本太小 | 只作探索来源，不进入新表格/显著性/选层 |
| GSM8K-500 W4/SG/FP16 准确率与配对统计 | 否；不是全集且与旧选层存在重叠 | 不删除原文件，但修改稿全部由 v4 全集结果替换 |
| 旧跨模型“转移层”结果 | 否；不能证明目标模型原生敏感性 | 用每模型 native screen 全部替换 |
| 旧校准/量化数字 | 原则上否；校准构造、共享精度库、种子与结果契约已改变 | v4 三种子重跑，不与旧 run 平均或拼接 |
| 旧相关性/误差传播图 | 只能作为提出假设的探索材料 | 新图必须来自 v4 teacher-forced 干预；结果不支持则降级措辞 |
| 方法实现、答案解析器、配对统计思想 | 可以复用代码思想，不能复用旧数值 | 已加版本、数据、模型、batch 和完整 ID 契约后重新执行 |
| 历史负结果 | 可在讨论中说明探索背景，但不是新边界结论 | 新协议若仍为负，必须如实保留并以新数值表述 |

“覆盖”是覆盖修改稿中的数值与结论，不是覆盖磁盘上的历史文件。历史目录保留可追溯性；v4 使用独立输出目录，避免两套数字混淆。

## 两张 RTX 3090 的冻结执行设置

- 两卡运行两个相互独立的单卡进程；不使用数据并行，也不把一个 batch 跨卡拆分。
- 31 GiB 内存模式下设置 `REVISION_FULL_MAX_CONCURRENT_RAM_BUILDERS=1` 和 `REVISION_FULL_MIN_AVAILABLE_RAM_GIB=24`：两进程可并行评估，但 `build-screen-bank` / `build-bank` 由跨进程文件锁串行。该调度不改变样本、量化方法、种子、batch、指标或实验矩阵。
- 初始值为每卡生成 batch `4`、四选一 item batch `2`、`max_new_tokens=256`。双卡不意味着每进程 batch 翻倍。
- 四模型 train-only smoke 全部通过后才写协议锁。如果任一 OOM，统一降到 `2/1`，删除尚未产生的 v4 协议锁并重新 `prepare --force`；正式样本产生后禁止改 batch。
- 建议分片：GPU0 `gemma2 qwen15`，GPU1 `smollm qwen05`。Gemma 没有 60 组随机分配全集评估，因此与 Qwen-1.5B 配对；SmolLM 与含额外因果诊断的 Qwen-0.5B 配对，比只按模型大小分片更接近实际总推理负载。
- 两进程的运行日志、状态文件、样本、状态和清理收据均按模型隔离。模型/数据缓存只读共享。

## 储存与运行时间影响

按当前四模型权重形状估计，双进程最坏同时活跃的“精度库+一个物化状态”为 `9.90 + 7.88 = 17.78 GiB`。预检给出的同一文件系统门槛是：

- 硬下限：约 `55 GiB` **实际空闲空间**；低于此值拒绝启动双进程。
- 建议值：约 `92 GiB` **实际空闲空间**；包含持久结果和安全余量。
- `100 GiB 总配额` 不等于 `100 GiB 空闲`。模型（约 11.9 GiB）、环境和数据缓存落盘后，必须重新运行 `df` 和预检。

这里的 GiB 空闲量均指磁盘；`free -h` 中约 31 GiB 的 `Mem` 才是系统内存。两张 3090 的各 24 GiB 显存、8 GiB swap 和 `/data` 的 103 GiB 可用空间都不能相互替代。低内存调度只串行高峰 builder，仍允许两张卡同时进行长时间评估。

生命周期清理不会在每一步重算量化。它只校验小型证据文件、写哈希收据并删除可重建 `.pt`，相对多日 GPU 推理开销很小。模型权重和数据 cache 的完整哈希在正式启动前读取一次，可能增加数分钟并产生共享盘 I/O；这是防止跑几天后才发现文件损坏的必要成本。不要对已有有效状态使用 `--force`，因为强制重建时旧文件与临时文件可能短时并存。

TaCQ 官方流程会生成接近 checkpoint 尺度的重要性文件，不计入上述内部 core 峰值。外部基线应在 core 完成后、模型逐个顺序运行，优先使用额外 scratch；只有在 1,319 条 canonical 样本、配置和哈希持久化后才删除其重要性/临时 checkpoint。外部适配失败不会使已通过 v4 契约的 core 数字重跑，但会阻止投稿级 gate 通过。

## 正式启动前 GO / NO-GO

必须全部满足：

1. 服务器实际可见两张 24-GiB GPU；若系统只有约 31 GiB RAM，必须启用单 RAM-builder 模式，启动/建库前 `MemAvailable >= 24 GiB`，且预检报告低内存模式通过。64/96 GiB 仅对应允许两个高内存 builder 同时运行的下限/建议值。
2. 四模型均由服务器下载器固定 commit，所有权重 hash 通过。
3. 核心与八个 panel 任务均已下载、记录 cache hash，并在 offline 模式可解析。
4. 四模型 smoke 在最终 batch 设置下通过。
5. `server_preflight.py --expected-gpus 2 --concurrent-models 2` 输出 `"ready": true`。
6. 31-GiB 主机已单独完成 `gemma2/c41` 正式 precision-bank pilot，并在该文件落盘后再次通过双卡 preflight；该银行随后原样复用，不是废弃试跑。
7. 两个 shard 无模型重叠，使用同一 protocol lock、数据 manifest 和环境变量。
8. 不存在要保留的旧 v4 部分输出；如存在，先让 readiness/断点校验决定是否可续跑，不手工拼文件。

任何一项不满足均为 **NO-GO**。代码无法提前保证第三方仓库适配或真实 GPU 驱动行为，但 smoke、预检、精确结果契约和阶段 gate 会把这些问题暴露在昂贵矩阵开始前，或阻止错误结果被误当成完成。
